#!/bin/sh

. /lib/functions/uci-defaults.sh

case "$(board_name)" in
"xunlong,orangepi-lite")
	[ "$(uci -q get wireless.@wifi-device[0].disabled)" = "1" ] && uci set wireless.@wifi-device[0].disabled=0
	;;
esac

exit 0